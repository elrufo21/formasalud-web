import React from "react";
import LoginRegister from "@/components/auth/LoginRegister";

export default function LoginPage() {
  return (
    <main className="flex-1 py-16 flex items-center justify-center min-h-[50vh]">
      <LoginRegister />
    </main>
  );
}
